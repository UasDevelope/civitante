import 'package:flutter/material.dart';

import '../../../utilse/widgets.dart';

class CommunityScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: AppText(text: "CommunityScreen"),
      ),
    );
  }
}
